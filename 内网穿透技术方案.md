# 内网穿透技术方案

让家中电脑通过公网主机实现远程访问的完整技术方案。

## 📊 方案对比

| 方案 | 类型 | 免费额度 | 特点 | Stars |
|------|------|----------|------|-------|
| **FRP** | 开源自建 | ✅ 完全免费 | 最成熟，TCP/UDP/HTTP | 92k ⭐ |
| **NPS** | 开源自建 | ✅ 完全免费 | Web管理界面 | 29k ⭐ |
| **ngrok** | 商业服务 | 有限制 | 即开即用 | - |
| **Cloudflare Tunnel** | 免费 | ✅ 完全免费 | Zero Trust 最安全 | - |

## 🏠 推荐方案：FRP 自建

### 架构图

```
┌─────────────┐     公网服务器      ┌─────────────┐
│  家中电脑   │ ◄─────────────────► │  FRP Server │
│ (FRP Client)│   端口映射         │  (VPS)      │
└─────────────┘                    └─────────────┘
        ▲                                    ▲
        │                                    │
        └──────── 远程访问 ◄────────────────┘
```

### 1. 服务端部署（VPS）

```bash
# 下载 FRP
wget https://github.com/fatedier/frp/releases/download/v0.52.3/frp_0.52.3_linux_amd64.tar.gz
tar -zxf frp_0.52.3_linux_amd64.tar.gz
cd frp_0.52.3_linux_amd64

# 配置 frps.ini
cat > frps.ini << 'EOF'
[common]
bind_port = 7000
dashboard_port = 7500
token = your_secret_token_here
dashboard_user = admin
dashboard_pwd = your_dashboard_password
EOF

# 启动服务
./frps -c frps.ini
```

### 2. 客户端部署（家中电脑）

```bash
# 同样下载并解压 FRP
wget https://github.com/fatedier/frp/releases/download/v0.52.3/frp_0.52.3_linux_amd64.tar.gz
tar -zxf frp_0.52.3_linux_amd64.tar.gz
cd frp_0.52.3_linux_amd64

# 配置 frpc.ini
cat > frpc.ini << 'EOF'
[common]
server_addr = your_vps_ip
server_port = 7000
token = your_secret_token_here

# SSH 远程访问
[ssh]
type = tcp
local_ip = 127.0.0.1
local_port = 22
remote_port = 6000

# 访问家中 Web 服务
[web]
type = http
local_ip = 127.0.0.1
local_port = 8080
custom_domains = your-domain.com
EOF

# 启动客户端
./frpc -c frpc.ini
```

### 3. 配置 systemd 开机自启

```bash
# 服务端
sudo cp frps.service /etc/systemd/system/
sudo systemctl enable frps
sudo systemctl start frps

# 客户端
sudo cp frpc.service /etc/systemd/system/
sudo systemctl enable frpc
sudo systemctl start frpc
```

## 🌐 备选方案：NPS

### 特点
- 更友好的 Web 管理界面
- 支持更多协议
- 配置可视化

```bash
# 服务端
wget https://github.com/ehang-io/nps/releases/latest/nps-linux_amd64.tar.gz
tar -xf nps-linux_amd64.tar.gz
sudo ./nps install
sudo nps start
```

访问 `http://your_vps_ip:8080` 管理界面

## ☁️ Cloudflare Tunnel（完全免费）

### 优点
- ✅ 完全免费
- ✅ 不需要公网 IP
- ✅ 最安全（Zero Trust）

```bash
# 1. 安装 cloudflared
wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -O /usr/local/bin/cloudflared
chmod +x /usr/local/bin/cloudflared

# 2. 登录
cloudflared tunnel login

# 3. 创建隧道
cloudflared tunnel create my-home-tunnel

# 4. 配置隧道
cloudflared tunnel route ip add --cidr 192.168.1.0/24 my-home-tunnel

# 5. 启动
cloudflared tunnel run --url ssh://localhost:22 my-home-tunnel
```

## 🔐 安全建议

1. **使用强 token**
2. **限制端口范围**
3. **启用 TLS 加密**
4. **使用 Cloudflare 代理隐藏真实 IP**

## 📚 参考链接

- [FRP 官方文档](https://github.com/fatedier/frp)
- [NPS 官方文档](https://github.com/ehang-io/nps)
- [Cloudflare Tunnel 文档](https://developers.cloudflare.com/tunnel/)
